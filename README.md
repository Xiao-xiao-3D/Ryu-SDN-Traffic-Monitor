# Ryu-SDN-Traffic-Monitor
Real-time SDN traffic monitoring &amp; visualization system built on Ryu controller. Features: port/flow statistics, business traffic classification (ICMP/HTTP/TCP/UDP), anomaly alerts (high traffic, burst, errors), REST API, and Flask-based dashboard with ECharts.
